import React from "react";

export const PhoneContext = React.createContext<any[]>([]);
export const SneakersContext = React.createContext<any[]>([]);
export const CartContext = React.createContext<any[]>([]);
